name: tests

on:
  push:
  workflow_dispatch:

env:
  ARCHIVE_DIR: /tmp

jobs:
  run-tests:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v3
    - run: |
        vagrant up
        vagrant ssh -- sudo -E CI=1 gotestsum --junitfile build/test/result.xml -- -timeout=15m . ./api
